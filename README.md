# muTau-PlatformIO

Compatible PlatformIO-style firmware project for the muTau-RV32-SoC.

This project builds a bare-metal RISC-V firmware using the LiteX-generated
headers, linker scripts, and libraries from the `base` SoC repository and its
`litex` submodule.
